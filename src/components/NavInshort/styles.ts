import { Styles } from "../types/common";

const styles: Styles = {
  wrapper: {
    display: "flex",
    alignItems: "center",
    height: "71px",
    width: "100%",
    boxShadow: "0px 1px 8px #888888",
  },

  drawer: {
    flex: "0.5",
    cursor: "pointer",
  },

  logo: {
    height: "100%",
    cursor: "pointer",
  },
};

export default styles;
